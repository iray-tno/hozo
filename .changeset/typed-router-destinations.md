---
'@hozo/navigation': minor
---

Add opt-in typed destination primitives for Expo Router and TanStack Router, including dynamic route parameters, object destinations, external URLs, and an explicit computed-URL escape hatch.
