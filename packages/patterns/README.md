# @hozo/patterns

Accessible composite UI patterns built from `@hozo/behaviors`.

This package owns Dialog, Tabs, Menu, Listbox, Combobox, RadioGroup, Toolbar, Tree, and Tooltip.
`@hozo/core` re-exports them as the convenient application facade; the behaviors package remains
headless and reusable.
