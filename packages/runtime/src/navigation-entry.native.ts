// Keep the subpath resolvable through package `react-native` conditions.
// The contract and context are platform-neutral; this file exists because
// every runtime subpath names its Native target explicitly in the generated
// package exports map.
export * from './navigation-entry.ts'
