[package]
name = "hozo_web"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
readme = "README.md"
description = "Hozo IR to DOM/CSS/ARIA lowering (Web backend)."
keywords = ["react-native", "css", "aria", "compiler", "web"]
categories = ["compilers", "web-programming"]

[dependencies]
hozo_ir.workspace = true
# Not just for tests: the fallback path emits CSS for candidate classes
# found by `hozo_parser::scan_class_candidates`.
hozo_parser.workspace = true
