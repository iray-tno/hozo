import { Section, View, Text } from '@hozo/core'

# Notes written in MDX

The block layout puts the table behind a visible border, and the grid stays
hidden until the isolate scrolls into view. None of those words is a class
anybody wrote, and none of them should reach the stylesheet.

<Section className="p-8 bg-brand">
  <View className="mt-6 flex gap-2">
    <Text className="text-3xl font-bold">A primitive written inline in MDX</Text>
  </View>
</Section>
