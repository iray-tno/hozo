# Approved from screen-readers run 34951455002 (VoiceOver, Safari).
# Not approved: VoiceOver said "Repository table Row 4 of 12 selected" where
# NVDA says "tree view". Whether the markup or WebKit's mapping is responsible
# is an open question, and approving the phrase would lock it in.
heading level 2 Tree
hozo_web
