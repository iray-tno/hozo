{
  "name": "login-demo",
  "private": true,
  "version": "0.0.0",
  "description": "Phase 0 benchmark app -- exercises @hozo/core through @hozo/vite end to end.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vite build && node scripts/check-production.mjs && vite build --ssr src/ssr.tsx --outDir dist-ssr && node scripts/check-ssr.mjs"
  },
  "dependencies": {
    "@hozo/core": "workspace:*",
    "@hozo/primitives": "workspace:*",
    "@hozo/runtime": "workspace:*",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@hozo/vite": "workspace:*",
    "@vitejs/plugin-react": "^6.0.5",
    "vite": "^8.2.1"
  }
}
