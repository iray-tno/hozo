heading, DateRangePicker, level 2
paragraph
Nothing chosen yet, so the trigger reads its placeholder. There is no Done button: the grid reports only a range with both ends, so the press that completes it is the press that finished the job.
end of paragraph
button, Dates of stay, not expanded, has popup dialog
Select dates
end of button, Dates of stay, not expanded, has popup dialog
